---
"Alexaka1.Analyzers.StructuredLogging": patch
---

Withhold a template-property code fix when all deterministic numeric suffixes are already in use instead of generating a random replacement name.
